/* Contar quantidade de caracteres reptidos em um nome */
const input:string = prompt("Insira uma string:")

function getLetrasRepetidas(input:string) {
	for(let i:number = 0; i < input.length; i++) {

	}	
}

getLetrasRepetidas(input)
